<%= form_with model: event do |form| %>
  <label for="meeting-time">Choose a time for your event:</label>
  <%= form.text_field :time, type: "datetime-local", value: event.time ? event.time.strftime("%Y-%m-%dT%H:%M") : "" %>

  <div>
    <%= form.label :title %><br>
    <%= form.text_field :title %>
    <% event.errors.full_messages_for(:title).each do |message| %>
      <div><%= message %></div>
    <% end %>
  </div>

  <div>
    <%= form.label :description %><br>
    <%= form.textarea :description %><br>
    <% event.errors.full_messages_for(:description).each do |message| %>
      <div><%= message %></div>
    <% end %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>